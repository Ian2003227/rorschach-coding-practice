# Raw/plaintext item bank must NEVER be committed (copyrighted test material + answer key)
data/items.json
data-raw/
pages/
*.pdf

.DS_Store
node_modules/

# Raw location-map JPGs are embedded (base64) into the encrypted blob at build time —
# the plaintext copies must not be committed (copyrighted workbook page content).
assets/loc/*.jpg
